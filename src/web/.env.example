# Build-time configuration for `vite dev` / `vite build`.
# Note these are baked into the bundle at build time — see the runtime /config.js
# mechanism the API serves for anything that has to differ per deployment.
VITE_SENTRY_DSN=your_sentry_dsn
VITE_ENVIRONMENT=development
